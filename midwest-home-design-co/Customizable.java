interface Customizable {
   
   // allows users to edit the default house templates or their own custom house template
   void customize(String style, int numBedrooms, int numBathrooms, double totalArea);
   
}
